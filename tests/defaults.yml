parameters:
  rbac:
    serviceaccounts:
      foo/bar:
        metadata:
          labels:
            foo: "false"
    clusterroles:
      raw:
        rules:
          - apiGroups:
              - ""
            resources:
              - namespaces
            verbs:
              - create
    clusterrolebindings:
      raw:
        roleRef:
          apiGroup: rbac.authorization.k8s.io
          kind: ClusterRole
          name: raw
        subjects:
          - kind: ServiceAccount
            name: bar
            namespace: foo
    roles:
      foo/raw:
        rules:
          - apiGroups:
              - ""
            resources:
              - configmaps
            verbs:
              - create
    rolebindings:
      foo/raw:
        roleRef:
          apiGroup: rbac.authorization.k8s.io
          kind: Role
          name: raw
        subjects:
          - kind: ServiceAccount
            name: bar
            namespace: foo
