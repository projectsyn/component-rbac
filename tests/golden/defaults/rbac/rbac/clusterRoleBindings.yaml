apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  annotations: {}
  labels:
    name: raw
  name: raw
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: raw
subjects:
  - kind: ServiceAccount
    name: bar
    namespace: foo
