apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  annotations: {}
  labels:
    name: helper
  name: helper
rules:
  - apiGroups:
      - ''
    resources:
      - namespaces
    verbs:
      - create
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  annotations: {}
  labels:
    name: raw
  name: raw
rules:
  - apiGroups:
      - ''
    resources:
      - namespaces
    verbs:
      - create
